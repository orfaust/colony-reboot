// Presentation constants of the game. Keep them in sync with the Odin sources.

/** Screen units per world unit, with the world origin at the viewport centre (src/app/presentation.odin WORLD_SCALE). */
export const WORLD_SCALE = 64;

/** Default window size in screen units (src/render/window.odin InitWindow). */
export const GAME_WINDOW = { width: 1280, height: 720 };
